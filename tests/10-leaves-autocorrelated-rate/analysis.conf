# Sample analysis file.

# The following variables are mandatory.
analysis_name="test-10-leaves"
rooted_tree="data/time.alpha.tree"
trees="data/test.treelist"

# Further, calibrations and constraints can be set using the following variables.
calibrations="data/calibrations.csv"
# constraints="Constraints.csv"
# braces="Braces.json"

# Finally, a suffix can be provided to distinguish between results.
# suffix="test"
